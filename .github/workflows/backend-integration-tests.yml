name: backend-integration-tests

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

concurrency:
  group: backend-backend-integration-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: "1"
      DOTNET_NOLOGO: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
          cache: true
          cache-dependency-path: |
            backend/**/packages.lock.json
            backend/**/*.csproj

      - name: Restore
        run: dotnet restore ./RealEstate.slnx

      - name: Build
        run: dotnet build ./RealEstate.slnx --configuration Release --no-restore

      - name: Test (integration)
        run: |
          dotnet test ./RealEstate.Tests/Integration/Integration.Tests.csproj --configuration Release --no-build --logger "trx;LogFileName=integration_test_results.trx"
          dotnet test ./RealEstate.Tests/Infrastructure/Infrastructure.Tests.csproj --configuration Release --no-build --logger "trx;LogFileName=integration_test_results.trx"

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-results
          path: backend/**/TestResults/*.trx
