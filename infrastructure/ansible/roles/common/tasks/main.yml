- name: Apt update
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600      # do not update cache if it is younger than one hour

- name: Install base packages
  ansible.builtin.apt:
    name:
      - curl
      - ca-certificates
      - gnupg
      - jq
    state: present
