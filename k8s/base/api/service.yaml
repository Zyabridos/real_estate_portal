apiVersion: v1
kind      : Service
metadata:
  name: realestate-api-svc
spec:
  type: ClusterIP
  selector:
    app: realestate-api
  ports:
    - name      : http
      port      : 80   # realestate-api-svc:80
      targetPort: 5000 # container api - pod-ip:5000
