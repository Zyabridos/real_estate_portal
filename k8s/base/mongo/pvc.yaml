apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: realestate-mongo-pvc
  labels:
    app: realestate-mongo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
