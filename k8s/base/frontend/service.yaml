apiVersion: v1
kind: Service
metadata:
  name: realestate-frontend-svc
  namespace: realestate
spec:
  type: ClusterIP
  selector:
    app: realestate-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
